# 大模型量化误差机理报告

> \*\*主题：大模型架构—量化误差耦合作用机制\*\*  
> \*\*版本：V1.0\*\*  
> \*\*日期：2026-07-30\*\*

\---

## 摘要

大模型量化并不是简单地把 FP16/BF16 参数映射为 INT8、INT4 或更低比特。真正决定量化后模型性能的，是**量化误差与模型架构之间的耦合作用**：同样大小的数值误差，注入到不同层、不同通道、不同算子、不同 token 位置后，可能产生完全不同的输出影响。

从机理上看，大语言模型中的量化误差主要经过五类结构发生耦合和放大：

1. **线性层的高维投影与通道不均匀性**：权重或激活中的少数离群通道决定量化动态范围，使普通均匀量化对“大多数正常值”分辨率不足；
2. **Attention 的乘性结构与 Softmax 非线性**：Q、K 误差通过内积形成交叉项，并进一步通过 Softmax 改变注意力概率分布；
3. **残差连接的跨层累积**：单层误差虽然较小，但会作为后续层输入持续传播，形成深层网络中的误差积累；
4. **归一化和门控结构的误差重分配**：RMSNorm/LayerNorm 会改变误差的相对尺度，SwiGLU/GeGLU 等乘性门控则可能放大量化扰动；
5. **自回归与 KV Cache 的时间累积**：量化误差不仅沿网络深度传播，还会沿生成时间传播，使长上下文场景中的 KV Cache 量化具有独特的敏感性。

因此，LLM 量化的核心问题可以概括为：

> \*\*量化误差并非独立噪声，而是被 Transformer 的结构、数据分布和自回归状态共同调制的结构化扰动。\*\*

从工程角度看，这解释了为什么低比特量化通常需要采用**分组量化、通道级缩放、离群值保护、旋转变换、Hessian/激活感知误差补偿、混合精度和结构感知量化**，而不能仅依靠简单的逐张量线性量化。

\---

# 1\. 问题定义

## 1.1 什么是量化误差

对于浮点变量 (x)，线性量化通常表示为

\[
q=\\operatorname{clip}\\left(\\operatorname{round}\\left(\\frac{x}{s}\\right)+z,q\_{\\min},q\_{\\max}\\right)
]

反量化结果为

\[
\\hat{x}=s(q-z)
]

于是量化误差定义为

\[
e\_x=\\hat{x}-x
]

如果没有截断，并假设舍入误差近似均匀分布，则有

\[
e\_x \\in \\left\[-\\frac{s}{2},\\frac{s}{2}\\right]
]

但在真实大模型中，这个“均匀独立噪声”假设往往并不成立，因为：

* 激活和权重分布通常非均匀；
* 存在明显离群值；
* 不同通道动态范围差异很大；
* 不同 token 的激活尺度差异明显；
* 量化误差会与输入、权重以及网络结构相关；
* clipping 会产生有偏误差；
* 多层网络中误差会被后续 Jacobian 进一步变换。

因此更准确的分析方式是把量化过程写为

\[
\\hat{\\mathbf{x}}=\\mathbf{x}+\\boldsymbol{\\epsilon}
]

并研究扰动 (\\boldsymbol{\\epsilon}) 如何经过 Transformer 的计算图传播。

\---

# 2\. 大模型中的量化对象

典型 decoder-only Transformer 中可量化对象主要包括：

|对象|典型量化方式|主要风险|
|-|-|-|
|Embedding|W8/W4|词向量细粒度信息丢失|
|Q/K/V 投影权重|W8/W4|Attention 分数扰动|
|Q/K/V 激活|A8/A4|离群通道导致误差增加|
|Attention 输出投影|W8/W4|残差分支误差注入|
|FFN / MLP 权重|W8/W4/W3/W2|参数规模最大，误差贡献显著|
|FFN 激活|A8/A4|SwiGLU 门控、离群值敏感|
|RMSNorm/LayerNorm|通常保留高精度|数值范围小但结构敏感|
|LM Head|W8/W4 或 FP16|Logit 排序敏感|
|KV Cache|KV8/KV4/KV2|长上下文与自回归累积|
|MoE Experts|混合精度|专家敏感性差异|
|Router/Gate|通常高精度|路由离散决策高度敏感|

量化设计的关键不是“是否量化”，而是：

\[
\\boxed{\\text{量化对象}+\\text{量化粒度}+\\text{误差传播位置}+\\text{结构敏感性}}
]

\---

# 3\. 统一的“架构—误差耦合”分析框架

设第 (l) 层 Transformer 的映射为

\[
\\mathbf{h}\_{l+1}=F\_l(\\mathbf{h}\_l;\\mathbf{W}\_l)
]

量化后：

\[
\\hat{\\mathbf{h}}\_{l+1}
===

F\_l(\\mathbf{h}\_l+\\delta\\mathbf{h}\_l;
\\mathbf{W}\_l+\\Delta\\mathbf{W}\_l)
]

在小扰动条件下，一阶展开可得

\[
\\delta\\mathbf{h}*{l+1}
\\approx
\\mathbf{J}*{h,l}\\delta\\mathbf{h}*l
+
\\mathbf{J}*{W,l}\\Delta\\mathbf{W}\_l
]

其中

\[
\\mathbf{J}\_{h,l}
===

\\frac{\\partial F\_l}{\\partial \\mathbf{h}*l},
\\qquad
\\mathbf{J}*{W,l}
===

\\frac{\\partial F\_l}{\\partial \\mathbf{W}\_l}
]

经过 (L) 层以后，早期层误差近似变为

\[
\\delta\\mathbf{h}*{L}
\\approx
\\left(
\\prod*{j=l+1}^{L-1}
\\mathbf{J}*{h,j}
\\right)
\\mathbf{J}*{W,l}\\Delta\\mathbf{W}\_l
]

这给出了一个非常重要的结论：

> \*\*某个参数的量化敏感性，不仅由该参数本身的量化误差决定，还取决于它后面的网络 Jacobian。\*\*

因此：

* “权重 MSE 最小”不等于“模型输出误差最小”；
* 不同层即使具有相同权重量化误差，其最终性能影响也可能完全不同；
* 需要根据激活、Hessian、梯度或输出重构误差判断量化敏感度。

GPTQ、AWQ、SmoothQuant、旋转量化以及混合精度量化，本质上都可以理解为在不同层面降低这一传播链中的有效误差。

\---

# 4\. 线性层：最基础的误差耦合机制

对于线性层：

\[
\\mathbf{y}=\\mathbf{x}\\mathbf{W}
]

量化后：

\[
\\hat{\\mathbf{x}}=\\mathbf{x}+\\Delta\\mathbf{x}
]

\[
\\hat{\\mathbf{W}}=\\mathbf{W}+\\Delta\\mathbf{W}
]

于是

\[
\\hat{\\mathbf{y}}
===

(\\mathbf{x}+\\Delta\\mathbf{x})
(\\mathbf{W}+\\Delta\\mathbf{W})
]

展开：

\[
\\Delta\\mathbf{y}
===

\\mathbf{x}\\Delta\\mathbf{W}
+
\\Delta\\mathbf{x}\\mathbf{W}
+
\\Delta\\mathbf{x}\\Delta\\mathbf{W}
]

低误差条件下忽略二阶项：

\[
\\boxed{
\\Delta\\mathbf{y}
\\approx
\\mathbf{x}\\Delta\\mathbf{W}
+
\\Delta\\mathbf{x}\\mathbf{W}
}
]

这说明：

### 4.1 权重量化误差受激活放大

如果某个输入通道 (x\_i) 很大，那么对应权重通道中的很小误差也会产生较大的输出误差。

因此：

\[
|\\Delta y|
\\not\\propto
|\\Delta W|
]

更合理的是：

\[
|\\Delta y|
\\propto
|x||\\Delta W|
]

这也是 **AWQ（Activation-aware Weight Quantization）** 的基本思想之一：判断权重重要性时，不只看权重本身，而要观察对应激活。

AWQ 的实验发现表明，少量显著权重通道对量化精度具有不成比例的重要性，因此通过激活统计识别并保护这些通道可以显著降低低比特权重量化损失。

### 4.2 激活量化误差受权重放大

类似地：

\[
\\Delta y\_A
===

\\Delta x W
]

当某个输出方向的权重范数很大时，相同的激活量化误差会产生更大的输出扰动。

因此不同通道的量化误差不能简单视为等价。

\---

# 5\. 离群值：LLM 低比特量化的核心矛盾之一

Transformer 大模型中一个典型现象是：

> 少数激活通道具有远高于普通通道的幅值。

假设一个张量中绝大多数值分布在

\[
\[-1,1]
]

但少数 outlier 达到

\[
\[-20,20]
]

如果使用逐张量 INT8 量化：

\[
s=\\frac{20}{127}
]

则量化步长约为

\[
0.157
]

大量位于 (\[-1,1]) 的普通值只能使用很少的量化格点表达，导致有效分辨率下降。

因此 outlier 有两个影响：

1. **扩大量化动态范围**；
2. **降低正常值区域的量化分辨率**。

这种现象在低比特下尤其严重。

LLM.int8() 的重要观察就是：大规模 Transformer 中会出现系统性的离群特征维度。其方法将离群维度单独使用较高精度计算，而大多数元素使用 INT8。

\---

# 6\. SmoothQuant：架构等价变换视角下的误差迁移

SmoothQuant 的核心思想不是“消灭”误差，而是：

> \*\*把难量化的 activation 动态范围迁移到更容易量化的 weight 上。\*\*

对于线性层

\[
Y=XW
]

引入一个对角缩放矩阵 (S)：

\[
Y=(XS^{-1})(SW)
]

数学上完全等价。

定义：

\[
\\tilde{X}=XS^{-1}
]

\[
\\tilde{W}=SW
]

则

\[
Y=\\tilde{X}\\tilde{W}
]

选择合适的 (S)，可以减小激活离群通道的幅值，从而降低 activation quantization error。

因此 SmoothQuant 揭示了一个非常重要的结构性质：

> Transformer 的某些线性映射存在\*\*尺度等价自由度（scaling invariance）\*\*，可以利用这一自由度重新分配量化难度。

这类思想后来进一步发展为 rotation-based quantization。

\---

# 7\. Rotation：为什么旋转可以降低量化误差

量化误差高度依赖坐标系。

例如：

\[
x=\[100,1,1,\\ldots]
]

在原坐标系中存在一个明显 outlier。

如果通过正交矩阵 (R) 旋转：

\[
x'=xR
]

能量可能分散到多个维度。

对于正交矩阵：

\[
RR^T=I
]

因此：

\[
|xR|\_2=|x|\_2
]

也就是说：

* 信息能量没有改变；
* 但元素最大值可能下降；
* 分布变得更均匀；
* uniform quantization 更容易。

QuaRot 利用 Transformer 中可保持函数等价的旋转变换，把隐藏状态、FFN 激活、Attention 相关张量和 KV Cache 的离群值打散，使端到端低比特量化更稳定。

SpinQuant 进一步说明：**并不是任意旋转效果都一样**，可以通过学习旋转矩阵进一步最小化量化误差。

因此：

\[
\\boxed{
\\text{低比特量化性能}
\\neq
\\text{仅由 bit width 决定}
}
]

还取决于：

\[
\\boxed{
\\text{张量所在坐标基与数据分布的匹配程度}
}
]

\---

# 8\. Attention：量化误差的乘性放大机制

Self-Attention 为

\[
Q=XW\_Q,\\qquad
K=XW\_K,\\qquad
V=XW\_V
]

Attention score：

\[
S=\\frac{QK^T}{\\sqrt{d}}
]

Attention：

\[
A=\\operatorname{softmax}(S)
]

最终：

\[
Y=AV
]

如果：

\[
\\hat Q=Q+\\Delta Q
]

\[
\\hat K=K+\\Delta K
]

则：

\[
\\hat S
===

\\frac{(Q+\\Delta Q)(K+\\Delta K)^T}{\\sqrt d}
]

误差为

\[
\\Delta S
===

\\frac{
Q\\Delta K^T+
\\Delta QK^T+
\\Delta Q\\Delta K^T
}{\\sqrt d}
]

忽略二阶项：

\[
\\boxed{
\\Delta S
\\approx
\\frac{
Q\\Delta K^T+
\\Delta QK^T
}{\\sqrt d}
}
]

因此 Attention 与普通线性层不同：

> \*\*Q 和 K 的量化误差通过乘性内积耦合。\*\*

如果某个 Q 或 K 向量范数较大，则对另一侧的误差更加敏感。

\---

# 9\. Softmax：从连续数值误差到注意力模式改变

有了 score 扰动：

\[
S\\rightarrow S+\\Delta S
]

Softmax 输出：

\[
A\_i
===

\\frac{e^{S\_i}}{\\sum\_j e^{S\_j}}
]

其 Jacobian：

\[
\\frac{\\partial A\_i}{\\partial S\_j}
===

A\_i(\\delta\_{ij}-A\_j)
]

因此：

\[
\\Delta A
\\approx
J\_{\\text{softmax}}\\Delta S
]

关键问题不是 (\\Delta S) 的绝对大小，而是它是否改变了 token 之间的**相对排序和概率间隔**。

例如：

原始 score：

\[
\[5.0,4.9,1.0]
]

量化扰动：

\[
\[-0.1,+0.1,0]
]

变成：

\[
\[4.9,5.0,1.0]
]

虽然误差只有 0.1，但注意力最大位置发生交换。

因此 Attention 量化存在典型的：

\[
\\boxed{
\\text{小数值扰动}
\\rightarrow
\\text{概率分布变化}
\\rightarrow
\\text{语义路径改变}
}
]

这是一类“结构放大”。

\---

# 10\. V 量化与 Q/K 量化为什么性质不同

对于：

\[
Y=AV
]

V 的误差：

\[
\\hat V=V+\\Delta V
]

则

\[
\\Delta Y\_V=A\\Delta V
]

它主要表现为**输出值扰动**。

而 Q/K 的误差先进入：

\[
QK^T
]

再进入：

\[
\\operatorname{softmax}
]

所以 Q/K 误差可能改变“从哪个 token 读取信息”。

因此可把二者简单理解为：

* **Q/K 误差：改变信息路由；**
* **V 误差：改变被路由的信息内容。**

这也是 KV Cache 中 K 和 V 往往需要采用不同量化策略的重要原因。

\---

# 11\. KV Cache 的架构—量化误差耦合

自回归推理第 (t) 步：

\[
K\_{1:t},V\_{1:t}
]

会被缓存。

下一 token 的 attention：

\[
A\_t=
\\operatorname{softmax}
\\left(
\\frac{q\_tK\_{1:t}^T}{\\sqrt d}
\\right)
]

输出：

\[
y\_t=A\_tV\_{1:t}
]

KV Cache 量化有三个特殊性质：

## 11.1 误差生命周期长

权重量化误差每次计算重新使用，但 KV Cache 中某个历史 token 的误差可能持续影响后续大量 token。

## 11.2 上下文越长，误差来源越多

随着 (t) 增长：

\[
K\_{1:t},V\_{1:t}
]

规模线性增长。

因此长上下文场景中 KV Cache 的内存占用和量化质量都更加重要。

## 11.3 K 与 V 的统计结构不一样

KIVI 对多种 LLM 的 KV 分布分析指出：

* Key 更适合 **per-channel quantization**；
* Value 更适合 **per-token quantization**。

这说明：

\[
\\boxed{
K/V \\text{虽然都属于 KV Cache，但其最佳量化粒度并不相同}
}
]

根本原因是 K 主要参与跨 token 内积，而 V 主要作为注意力加权后的内容载体，两者统计分布和结构作用不同。

\---

# 12\. RoPE 与量化误差

RoPE 通常作用于 Q、K：

\[
Q' = R\_{\\theta}Q
]

\[
K' = R\_{\\theta}K
]

其中 (R\_\\theta) 是位置相关旋转。

在理想浮点情况下，RoPE 旋转保持二维子空间中的范数。

但在低比特量化中需要关注：

\[
Q
\\rightarrow
R\_\\theta Q
\\rightarrow
Q\_{\\text{quant}}
]

和

\[
Q
\\rightarrow
Q\_{\\text{quant}}
\\rightarrow
R\_\\theta Q\_{\\text{quant}}
]

二者误差特性并不完全相同。

尤其对于 K cache：

* 在 RoPE 前量化还是 RoPE 后量化；
* per-channel 统计是否仍稳定；
* 长位置下旋转后的元素分布如何变化；

都会影响最终量化精度。

因此 KV Cache 量化常常需要把 **RoPE 位置变换** 纳入结构设计，而不是仅把 K/V 当普通矩阵处理。

\---

# 13\. GQA / MQA 与 KV 量化耦合

标准 MHA：

* 每个 query head 都有独立 K/V head。

GQA：

* 多个 query head 共享一组 K/V head。

MQA：

* 所有 query heads 共享极少数甚至单个 K/V head。

共享会带来明显的量化效应：

> \*\*同一个 K/V 量化误差会同时影响多个 Query Head。\*\*

设共享 K：

\[
K^{(g)}
]

被 (m) 个 Q head 使用。

则：

\[
S\_i=Q\_i(K^{(g)})^T
]

若：

\[
K^{(g)}\\rightarrow K^{(g)}+\\Delta K
]

则所有相关 head 都出现：

\[
\\Delta S\_i=Q\_i\\Delta K^T
]

因此 GQA/MQA 下的 K/V 误差具有更明显的**共享误差效应**。

这意味着：

* KV Cache 更省内存；
* 但某些共享 K/V 通道的误差影响范围更大；
* 量化尺度和异常通道保护策略需要考虑 head-sharing 结构。

\---

# 14\. RMSNorm / LayerNorm：误差重标定机制

以 RMSNorm 为例：

\[
y=
\\frac{x}{\\sqrt{\\frac{1}{d}\\sum\_i x\_i^2+\\epsilon}}
\\odot g
]

如果输入：

\[
x\\rightarrow x+\\Delta x
]

则归一化因子本身也发生变化：

\[
r(x)
===

\\sqrt{
\\frac{1}{d}
|x|\_2^2+\\epsilon
}
]

因此量化误差不仅直接进入分子，也会影响分母。

这意味着：

\[
\\text{Norm}(x+\\Delta x)
---

\\text{Norm}(x)
]

并不等于简单的线性误差。

从结构上看，Normalization 有双重作用：

### 正面作用

它可以抑制某些整体尺度漂移，使后续层输入尺度相对稳定。

### 负面作用

它会把局部误差重新分配到整个 hidden dimension。

所以：

> \*\*Normalization 可以抑制绝对幅度误差，但可能把局部误差转化为方向误差。\*\*

在很多实际量化框架中，LayerNorm/RMSNorm 本身通常保持 FP16/BF16 或至少采用较高精度。

\---

# 15\. Residual Connection：量化误差的跨层累积

Pre-Norm Transformer 可写为：

\[
h'=
h+
\\operatorname{Attn}(\\operatorname{Norm}(h))
]

\[
h\_{\\text{next}}
===

h'+
\\operatorname{FFN}(\\operatorname{Norm}(h'))
]

如果 Attention 分支存在误差：

\[
\\hat{h}'
===

h+
\\operatorname{Attn}(\\operatorname{Norm}(h))
+
\\epsilon\_A
]

则：

\[
\\hat{h}'=h'+\\epsilon\_A
]

该误差不会在当前层结束，而是直接进入下一层。

经过多层：

\[
\\delta h\_L
===

\\epsilon\_1
+
J\_2\\epsilon\_1
+
\\epsilon\_2
+\\cdots
]

更严格地说，不同层误差都会被后续网络 Jacobian 传播。

因此深层 Transformer 的量化误差具有：

\[
\\boxed{
\\text{层间累积性}
}
]

但是 Residual 也提供了高精度 identity path，使量化分支并不是唯一的信息通路。

所以 Residual 同时具有两面性：

* 保留原始信息，增强鲁棒性；
* 让量化误差能够持续进入后续层。

\---

# 16\. FFN：为什么通常是权重量化的主要对象

典型 Transformer FFN：

\[
\\operatorname{FFN}(x)
===

W\_2\\sigma(W\_1x)
]

现代 LLM 常使用 SwiGLU：

\[
\\operatorname{SwiGLU}(x)
===

W\_2
\\left\[
\\operatorname{SiLU}(W\_gx)
\\odot
(W\_ux)
\\right]
]

FFN 参数量通常占 Transformer block 的主要部分，因此 W4/W3/W2 权重量化可以显著降低模型存储和内存带宽压力。

但 FFN 中存在一个重要机制：

\[
a=\\operatorname{SiLU}(W\_gx)
]

\[
b=W\_ux
]

\[
z=a\\odot b
]

若：

\[
a\\rightarrow a+\\Delta a
]

\[
b\\rightarrow b+\\Delta b
]

则：

\[
\\Delta z
===

a\\odot\\Delta b
+
b\\odot\\Delta a
+
\\Delta a\\odot\\Delta b
]

即：

\[
\\boxed{
\\text{SwiGLU 是一个典型乘性误差耦合结构}
}
]

某一支的高幅值会放大另一支的量化误差。

因此 W4A4 等极低激活精度场景往往比单纯 W4A16 更困难。

\---

# 17\. 激活函数与误差

对于非线性函数：

\[
y=f(x)
]

输入量化误差：

\[
x+\\Delta x
]

一阶近似：

\[
\\Delta y
\\approx
f'(x)\\Delta x
]

因此局部误差增益为：

\[
|f'(x)|
]

对于 SiLU：

\[
\\operatorname{SiLU}(x)=x\\sigma(x)
]

不同输入区域具有不同的误差敏感性。

因此激活量化误差不是固定增益传播，而是：

\[
\\boxed{
\\text{数据相关的非线性误差传播}
}
]

\---

# 18\. LM Head：为什么最后一层也可能敏感

最终 hidden state：

\[
h
]

logits：

\[
z=hW\_{\\text{vocab}}
]

概率：

\[
p\_i
===

\\frac{e^{z\_i}}{\\sum\_j e^{z\_j}}
]

如果 top-1 与 top-2 logit 间隔为

\[
m=z\_{(1)}-z\_{(2)}
]

当量化误差导致：

\[
|\\Delta z\_{(1)}-\\Delta z\_{(2)}|>m
]

则可能直接改变下一个 token。

所以：

> \*\*生成任务关心的不只是 logits 的 MSE，还关心 token 排序 margin。\*\*

一个看似很小的最后层误差，可能造成 token 分叉。

\---

# 19\. 自回归生成：误差从“数值扰动”升级为“轨迹分叉”

自回归模型：

\[
x\_{t+1}\\sim
P(
x\_{t+1}|x\_{\\le t}
)
]

如果量化导致第 (t) 步 token 从

\[
x\_t
]

变为

\[
x'\_t
]

那么后续模型输入序列已经不同：

\[
(x\_1,\\dots,x\_t)
\\neq
(x\_1,\\dots,x'\_t)
]

之后差异不再只是数值误差，而成为：

\[
\\boxed{
\\text{序列状态差异}
}
]

这可以称为**自回归轨迹分叉效应**。

因此：

* perplexity 的小幅变化；
* 某几个 token 概率的微小扰动；

在长生成过程中可能演化成完全不同的输出文本。

这也是评价量化模型时不能只看单层 reconstruction error 的原因。

\---

# 20\. Weight-only Quantization 为什么相对容易

W4A16 表示：

* 权重 INT4；
* activation 保持 FP16/BF16。

此时：

\[
\\Delta x\\approx0
]

线性层误差主要为：

\[
\\Delta y
\\approx
x\\Delta W
]

权重是静态的，因此可以离线进行：

* calibration；
* group-wise scaling；
* clipping；
* Hessian-aware reconstruction；
* activation-aware scaling；
* reorder；
* rotation。

因此 weight-only quantization 一般是目前工程部署中最成熟的低比特路径之一。

\---

# 21\. W4A4 为什么明显更难

W4A4 中：

\[
\\Delta y
===

x\\Delta W
+
\\Delta x W
+
\\Delta x\\Delta W
]

三个误差项同时存在。

此外 activation：

* 随 token 改变；
* 随 batch 改变；
* 不同层分布不同；
* outlier 显著；
* 不能完全离线固定。

所以 W4A4 的本质困难是：

\[
\\boxed{
\\text{动态数据分布}
+
\\text{权重误差}
+
\\text{激活误差}
+
\\text{结构非线性}
}
]

这也是 SmoothQuant、QuaRot、SpinQuant 等工作的关键应用背景。

\---

# 22\. GPTQ：从“权重误差”转向“输出重构误差”

简单 RTN：

\[
W\_q=Q(W)
]

只考虑：

\[
|W-W\_q|
]

GPTQ 类方法更关注量化以后线性层输出：

\[
XW
]

与

\[
XW\_q
]

之间的误差：

\[
|XW-XW\_q|\_2^2
]

展开：

\[
|X(W-W\_q)|\_2^2
]

定义：

\[
\\Delta W=W-W\_q
]

则：

\[
|X\\Delta W|^2
===

\\Delta W^T X^TX\\Delta W
]

其中：

\[
H\\approx X^TX
]

可以理解为局部二阶敏感度信息。

因此 GPTQ 的核心思想可以简化理解为：

> 不同权重误差对输出的影响不同，需要根据输入协方差/Hessian 对误差进行加权和补偿。

\---

# 23\. AWQ：显著通道保护

对于：

\[
y=XW
]

若第 (j) 个输入通道激活非常大：

\[
|X\_j|\\gg |X\_k|
]

那么该通道对应权重误差：

\[
\\Delta W\_j
]

会产生更大的输出误差。

因此权重重要性不能仅由：

\[
|W\_j|
]

决定，而应考虑：

\[
|X\_jW\_j|
]

或者相关激活统计。

AWQ 通过激活统计识别显著权重通道，并使用缩放方式保护这些通道。

从本文分析框架看，AWQ 实际解决的是：

\[
\\boxed{
\\text{激活幅值}
\\times
\\text{权重量化误差}
}
]

这一耦合项。

\---

# 24\. Group-wise Quantization 为什么有效

假设整个权重矩阵使用一个 scale：

\[
s=
\\frac{\\max|W|}{2^{b-1}-1}
]

只要矩阵存在一个很大的 outlier，所有元素的 scale 都被迫增大。

Group-wise Quantization 将矩阵划分为多个 group：

\[
W=
\[
W^{(1)},
W^{(2)},\\dots
]
]

每组使用：

\[
s\_g
]

这样局部动态范围更小。

因此量化误差通常满足：

\[
E\_{\\text{group}}
<
E\_{\\text{tensor}}
]

代价是：

* 更多 scale；
* metadata 增加；
* kernel 实现复杂；
* group size 过小可能降低计算效率。

因此 group size 是典型的：

\[
\\boxed{
\\text{精度—存储—kernel效率}
}
]

三者折中。

\---

# 25\. Per-channel / Per-token：本质是匹配结构统计特性

量化粒度越细：

\[
\\text{scale 数量越多}
]

通常量化误差越小。

常见粒度：

\[
\\text{per-tensor}
\\rightarrow
\\text{per-channel}
\\rightarrow
\\text{per-group}
]

激活还可以：

\[
\\text{per-token}
]

选择粒度的真正依据应是：

\[
\\boxed{
\\text{哪个维度上的统计分布相对稳定}
}
]

例如 KIVI 中：

* K：per-channel；
* V：per-token；

就是根据实际 KV Cache 分布得出的结构感知策略。

\---

# 26\. Symmetric 与 Asymmetric Quantization

对称量化：

\[
q=
\\operatorname{round}(x/s)
]

优点：

* 计算简单；
* kernel 友好；
* 零点为 0。

如果分布偏移明显：

\[
x\\in\[a,b],\\qquad a\\neq-b
]

asymmetric quantization：

\[
q=
\\operatorname{round}(x/s)+z
]

可以更充分利用有限编码区间。

但会增加：

* zero-point；
* 额外运算；
* kernel 复杂度。

因此是否使用 asymmetric，需要结合张量分布和硬件支持。

\---

# 27\. Clipping：量化误差的偏差—方差权衡

如果存在极少量 outlier：

\[
x\_{\\max}\\gg x\_{\\text{typical}}
]

保留完整动态范围会导致 scale 太大。

可以 clipping：

\[
x\_c
===

\\operatorname{clip}(x,-\\alpha,\\alpha)
]

量化误差可分解为：

\[
E
===

E\_{\\text{round}}
+
E\_{\\text{clip}}
]

其中：

* (\\alpha) 大：clipping error 小，但 rounding error 大；
* (\\alpha) 小：rounding error 小，但 clipping error 大。

最优 clipping 范围：

\[
\\alpha^\*
===

\\arg\\min\_\\alpha E(\\alpha)
]

因此 calibration 的核心任务之一，就是寻找合理动态范围。

\---

# 28\. 不同 Transformer 层为什么量化敏感性不同

同样使用 INT4，不同层性能损失不同，主要来自：

## 28.1 激活分布不同

不同层的：

* 最大值；
* 方差；
* kurtosis；
* outlier channel 数量；

均不同。

## 28.2 后续传播距离不同

较早层的误差需要经过更多层传播。

## 28.3 Jacobian 不同

有些层附近具有更大的局部误差增益。

## 28.4 功能角色不同

部分层更强地承担：

* lexical information；
* attention routing；
* reasoning；
* feature refinement；
* output projection。

因此 layer-wise uniform bit-width 并不一定最优。

\---

# 29\. 混合精度量化的理论动机

设每层量化误差为：

\[
E\_l(b\_l)
]

层敏感系数：

\[
\\lambda\_l
]

则整体目标可近似表示：

\[
\\min\_{b\_1,\\dots,b\_L}
\\sum\_l
\\lambda\_l E\_l(b\_l)
]

约束：

\[
\\sum\_l C\_l(b\_l)\\le B
]

其中：

* (b\_l)：第 (l) 层 bit width；
* (C\_l)：存储或计算成本；
* (B)：总预算。

于是 bit allocation 本质是：

\[
\\boxed{
\\text{在固定硬件预算下，把高精度分配给最敏感结构}
}
]

\---

# 30\. MoE 架构与量化误差耦合

MoE 层：

\[
y=
\\sum\_{e\\in TopK(x)}
p\_e(x)E\_e(x)
]

其中：

* (p\_e(x))：router probability；
* (E\_e(x))：expert；
* 只有少量专家被激活。

MoE 与 Dense LLM 的量化差异非常明显。

\---

## 30.1 Expert 量化误差不是均匀重要

不同 expert：

* 调用频率不同；
* 数据分布不同；
* 负责能力不同；
* 输出幅值不同。

因此：

\[
E\_1,E\_2,\\dots,E\_N
]

不应该天然使用相同 bit-width。

近年来的 MoE 量化研究也越来越倾向于 expert-wise mixed precision，即根据专家敏感性分配不同精度。

\---

## 30.2 Router 是离散决策敏感点

Router：

\[
p=\\operatorname{softmax}(W\_rx)
]

Top-K：

\[
\\mathcal E=TopK(p)
]

如果 router 量化导致两个 expert 的排序交换：

\[
p\_i>p\_j
]

变成：

\[
\\hat p\_i<\\hat p\_j
]

模型将调用完全不同的 expert。

因此：

\[
\\boxed{
\\text{Router 小误差}
\\rightarrow
\\text{Expert 离散切换}
}
]

这比普通线性层的连续误差更加危险。

因此 router 通常值得保留较高精度。

\---

## 30.3 Expert 量化与路由概率耦合

expert 输出：

\[
y=
\\sum\_e p\_eE\_e(x)
]

expert 误差：

\[
\\Delta E\_e
]

最终：

\[
\\Delta y
===

\\sum\_e p\_e\\Delta E\_e
]

所以被频繁激活、路由概率更大的 expert，其量化误差影响通常更大。

但也存在另一种情况：低频 expert 可能负责少见但关键能力，因此仅按访问频率分配精度也未必最优。

2026 年已有研究进一步从 expert 敏感度、router 训练变化以及专家内部方差等角度进行混合精度分配。

\---

# 31\. Attention Head 与误差耦合

Multi-head Attention：

\[
Y=
Concat(head\_1,\\dots,head\_H)W\_O
]

不同 head 可能：

* attention entropy 不同；
* 激活幅值不同；
* 功能重要性不同；
* 对 outlier 的敏感度不同。

因此：

\[
\\Delta head\_i
]

经过：

\[
W\_O
]

后，对输出的贡献不同。

这说明进一步的量化粒度可以做到：

* layer-wise；
* module-wise；
* head-wise；
* channel-wise。

但粒度越细，kernel 和 metadata 成本越高。

\---

# 32\. 长上下文为什么会放大 KV 量化问题

上下文长度：

\[
T
]

Attention score：

\[
S\\in\\mathbb{R}^{T}
]

随着 (T) 增加：

1. 参与比较的 token 更多；
2. 旧 token 的 KV 需要长期保留；
3. 量化误差来源增加；
4. attention score margin 可能变小；
5. KV Cache 内存逐渐成为主要瓶颈。

所以长上下文系统通常会更加重视：

* KV4/KV2；
* residual cache；
* sliding window；
* token importance；
* mixed precision KV；
* rotation；
* outlier channel separation。

\---

# 33\. 量化误差的四种“放大”

从整个 Transformer 看，可以把误差放大分成四类。

## 33.1 幅度放大

\[
\\Delta y=x\\Delta W
]

大激活放大权重量化误差。

\---

## 33.2 乘性放大

Attention：

\[
QK^T
]

SwiGLU：

\[
a\\odot b
]

均包含乘性耦合。

\---

## 33.3 非线性放大

Softmax、SiLU、Top-K routing 可以把小扰动变为明显输出变化。

\---

## 33.4 时序放大

Autoregressive decoding：

\[
x\_t
\\rightarrow
x\_{t+1}
\\rightarrow
x\_{t+2}
]

早期 token 改变会导致后续轨迹完全不同。

\---

# 34\. 量化误差的三个传播维度

可以建立一个统一视角：

## 维度一：网络深度

\[
Layer\_1
\\rightarrow
Layer\_2
\\rightarrow
\\cdots
\\rightarrow
Layer\_L
]

残差结构传播误差。

## 维度二：网络宽度

同一层：

\[
channel
\\rightarrow
head
\\rightarrow
group
]

离群通道和共享结构产生横向耦合。

## 维度三：生成时间

\[
token\_1
\\rightarrow
token\_2
\\rightarrow
\\cdots
\\rightarrow
token\_T
]

KV Cache 和 autoregressive sampling 产生时间耦合。

因此大模型量化实际上是一个：

\[
\\boxed{
\\text{深度 × 宽度 × 时间}
}
]

三维误差传播问题。

\---

# 35\. 架构模块敏感性总结

|模块|主要量化误差|误差放大机制|敏感性|
|-|-|-|-|
|Embedding|W error|输入直接扰动|中|
|Q projection|W/A error|QK 乘性 + Softmax|高|
|K projection|W/A/KV error|QK 乘性 + Cache 长期使用|高|
|V projection|W/A/KV error|Attention 加权|中高|
|Attention score|Q/K error|Softmax 排序变化|高|
|Output projection|W/A error|进入 residual|中高|
|RMSNorm|A error|全通道重标定|高敏感，常保高精度|
|FFN Up/Gate|W/A error|SwiGLU 乘性|高|
|FFN Down|W/A error|残差注入|中高|
|LM Head|W/A error|token ranking|高|
|KV Cache|K/V quant error|长上下文 + 时间累积|高|
|MoE Router|W/A error|Top-K 离散切换|极高|
|MoE Experts|W error|expert-specific sensitivity|不均匀|

\---

# 36\. Dense、GQA、MoE 架构量化差异

## Dense Transformer

核心问题：

* FFN 参数占比高；
* activation outlier；
* Attention Q/K 敏感；
* residual 层间累积。

适合：

* GPTQ；
* AWQ；
* SmoothQuant；
* rotation；
* group-wise W4。

\---

## GQA / MQA

额外问题：

* KV head 共享；
* 单个 K/V 误差影响多个 Q head；
* KV Cache 量化更加关键。

适合：

* K/V 分开设计量化粒度；
* KV mixed precision；
* residual KV；
* rotation。

\---

## MoE

额外问题：

* expert 数量大；
* expert 敏感性不同；
* router 存在离散切换；
* expert memory bandwidth 成为关键瓶颈。

适合：

* expert-wise mixed precision；
* router 高精度；
* activation-aware expert quantization；
* hot/cold expert 差异化量化。

\---

# 37\. 从“数值误差”到“任务性能”的链条

完整量化影响链可以表示为：

\[
\\text{Bit Width}
]

\[
\\downarrow
]

\[
\\text{Scale / Zero Point / Clipping}
]

\[
\\downarrow
]

\[
\\text{Weight / Activation / KV Error}
]

\[
\\downarrow
]

\[
\\text{Linear Projection Error}
]

\[
\\downarrow
]

\[
\\text{Attention / FFN Structural Coupling}
]

\[
\\downarrow
]

\[
\\text{Residual Error Propagation}
]

\[
\\downarrow
]

\[
\\text{Logit Perturbation}
]

\[
\\downarrow
]

\[
\\text{Token Probability / Ranking Change}
]

\[
\\downarrow
]

\[
\\text{Autoregressive Trajectory Divergence}
]

\[
\\downarrow
]

\[
\\boxed{\\text{Task Accuracy / Generation Quality Degradation}}
]

这条链路就是“大模型架构—量化误差耦合作用机制”的核心。

\---

# 38\. 为什么单纯最小化 Quantization MSE 不够

传统量化目标：

\[
\\min
|W-Q(W)|\_2^2
]

但模型真正关心的是：

\[
\\min
\\mathcal L(f\_Q(X),Y)
]

两者之间存在巨大差距。

更合理的层级目标包括：

### Level 1：权重误差

\[
|W-W\_q|^2
]

### Level 2：层输出误差

\[
|XW-XW\_q|^2
]

### Level 3：Block 输出误差

\[
|F(X)-F\_q(X)|^2
]

### Level 4：Logit 误差

\[
|z-z\_q|^2
]

### Level 5：任务损失

\[
\\Delta\\mathcal L
]

越靠后越接近真实目标，但 calibration 和优化代价越高。

\---

# 39\. 一个更统一的量化敏感度指标

对于参数组 (\\theta\_g)，设量化扰动：

\[
\\Delta\\theta\_g
]

损失函数二阶展开：

\[
\\Delta\\mathcal L
\\approx
\\nabla\\mathcal L^T\\Delta\\theta
+
\\frac{1}{2}
\\Delta\\theta^TH\\Delta\\theta
]

在预训练模型局部梯度较小时：

\[
\\nabla\\mathcal L\\approx0
]

于是：

\[
\\Delta\\mathcal L
\\approx
\\frac{1}{2}
\\Delta\\theta^TH\\Delta\\theta
]

因此可以定义：

\[
S\_g
===

\\Delta\\theta\_g^TH\_g\\Delta\\theta\_g
]

作为量化敏感度。

实际中 Hessian 太昂贵，可以用：

* activation covariance；
* Fisher information；
* gradient norm；
* reconstruction loss；
* output perturbation；
* perplexity change；

近似。

\---

# 40\. 量化误差与模型规模的关系

模型规模增加不意味着量化一定更困难。

大模型可能存在两种相反效应：

## 有利因素

* 冗余度更高；
* 多个表示路径可以补偿局部误差；
* 参数规模大，可容忍一定随机扰动。

## 不利因素

* emergent outlier 更明显；
* 层数更深；
* 低比特误差传播路径更长；
* 极端低比特下结构敏感性更加突出。

LLM.int8() 的研究特别指出，大规模模型会出现系统性的离群特征，这也是普通 INT8 量化在超大模型上遇到困难的重要原因。

因此：

\[
\\boxed{
\\text{模型规模}
\\rightarrow
\\text{量化鲁棒性}
}
]

不是单调关系。

\---

# 41\. Calibration Data 为什么重要

PTQ 通常使用少量 calibration data。

其作用不是重新训练，而是估计：

* activation range；
* channel importance；
* Hessian / covariance；
* clipping threshold；
* scale；
* rotation；
* layer sensitivity。

如果 calibration 数据与真实部署数据分布不一致：

\[
P\_{\\text{calib}}(X)
\\neq
P\_{\\text{deploy}}(X)
]

则量化参数可能不是最优。

因此量化也存在：

\[
\\boxed{
\\text{Calibration Distribution Shift}
}
]

问题。

AWQ 强调其激活感知方法不依赖反向传播，并试图提高跨域泛化；但在一般意义上，calibration set 的覆盖性仍然是 PTQ 的重要工程因素。

\---

# 42\. QAT 与 PTQ 的差异

## PTQ

先训练：

\[
W^\*
]

再量化：

\[
Q(W^\*)
]

模型训练时并不知道未来存在量化误差。

\---

## QAT

训练过程中模拟：

\[
W\_q=Q(W)
]

模型学习：

\[
\\min\_W
\\mathcal L(Q(W))
]

因此网络可以主动调整参数分布，使其更适合量化。

QAT 的优势是能够让：

* 权重；
* activation；
* normalization；
* residual；

共同适应量化误差。

但训练成本明显更高。

\---

# 43\. QLoRA 的误差机制

QLoRA 中基础模型权重使用 4-bit NF4 存储，但：

* 基础权重冻结；
* LoRA 参数保持较高精度训练；
* 前向时反量化参与计算；
* 梯度更新 LoRA 而不是量化基础权重。

形式：

\[
W\_{\\text{eff}}
===

\\hat W\_{\\text{4bit}}
+
BA
]

其中：

\[
\\hat W\_{\\text{4bit}}
===

W+\\Delta W\_q
]

LoRA 可以在一定程度上学习：

\[
BA
\\approx
-\\Delta W\_q+\\Delta W\_{\\text{task}}
]

因此从误差视角看，LoRA 不仅承担任务适配，也可能部分补偿冻结基础模型的量化扰动。

QLoRA 使用 NF4、double quantization 等设计降低微调内存开销，是低比特训练/微调的重要范式。

\---

# 44\. NF4 为什么适合神经网络权重

均匀 INT4 假设编码点均匀分布。

而神经网络权重通常近似集中于 0 附近。

NF4 采用非均匀的量化 level，使编码点更匹配近似正态分布权重。

其思想是：

\[
\\boxed{
\\text{量化码本应该匹配数据概率分布}
}
]

而不是只考虑动态范围。

这说明量化误差还与：

\[
\\text{Architecture}
+
\\text{Tensor Distribution}
+
\\text{Quantizer Codebook}
]

三者耦合。

\---

# 45\. 端到端 W4A4KV4 的真正困难

端到端低比特：

\[
W4A4KV4
]

同时量化：

* weight；
* activation；
* KV cache。

此时误差链包括：

\[
\\Delta W
]

\[
\\downarrow
]

\[
\\Delta Q,\\Delta K,\\Delta V
]

\[
\\downarrow
]

\[
\\Delta Attention
]

同时：

\[
\\Delta KV\_{\\text{history}}
]

持续进入未来 token。

因此 W4A4KV4 的难点不是三个独立量化问题相加，而是：

\[
\\boxed{
\\text{Weight}
\\times
\\text{Activation}
\\times
\\text{KV}
\\times
\\text{Transformer Structure}
}
]

形成耦合系统。

QuaRot、SpinQuant 等方法之所以采用全模型旋转，就是试图从整个计算图层面同时改善这些张量的低比特友好性。

\---

# 46\. 误差相关性：一个常被忽视的问题

传统噪声分析常假设：

\[
E\[\\epsilon]=0
]

且：

\[
E\[x\\epsilon]=0
]

但真实 deterministic quantization 中：

\[
\\epsilon=Q(x)-x
]

本身就是 (x) 的函数。

因此：

\[
E\[x\\epsilon]
]

一般不一定为 0。

特别是：

* clipping；
* asymmetric distribution；
* outlier；
* deterministic rounding；

都会产生结构化误差。

这也是为什么在真实 LLM 中，量化误差不能简单视为白噪声。

\---

# 47\. 架构等价变换是低比特量化的重要方向

SmoothQuant：

\[
XW
===

(XS^{-1})(SW)
]

Rotation：

\[
XW
===

(XR)(R^TW)
]

其中：

\[
R^TR=I
]

这些方法的共同点是：

\[
\\boxed{
\\text{不改变 FP 模型函数}
}
]

但改变：

\[
\\boxed{
\\text{量化后的误差分布}
}
]

因此未来低比特量化的一个核心思想是：

> 不一定直接优化 quantizer，也可以先改变模型表示基，使模型天然更加“quantization friendly”。

\---

# 48\. 架构—量化联合设计

传统流程：

\[
\\text{Train Model}
\\rightarrow
\\text{Quantize Model}
]

更理想的流程：

\[
\\text{Quantization-aware Architecture}
\\rightarrow
\\text{Train}
\\rightarrow
\\text{Quantize}
]

可以考虑：

* 降低 activation outlier；
* 限制通道动态范围；
* 使用更容易量化的 normalization；
* 减少极端 gate activation；
* 设计 quantization-friendly attention；
* 设计 KV-friendly positional encoding；
* 对 MoE router 保留高精度；
* 对高敏感模块采用 mixed precision。

这意味着量化不只是部署阶段问题，而可以成为模型架构设计约束。

\---

# 49\. 工程诊断方法

实际量化一个 LLM 时，建议依次做以下诊断。

## 49.1 Weight Distribution

统计每层：

\[
\\max|W|
]

\[
\\operatorname{std}(W)
]

\[
\\text{kurtosis}
]

以及 channel-wise range。

\---

## 49.2 Activation Distribution

统计：

\[
\\max|X|
]

per-token max：

\[
m\_t=\\max\_i|X\_{t,i}|
]

per-channel max：

\[
m\_i=\\max\_t|X\_{t,i}|
]

判断：

* token outlier；
* channel outlier；
* spike outlier。

\---

## 49.3 Layer Reconstruction Error

\[
E\_l
===

\\frac{
|Y\_l-\\hat Y\_l|\_F
}{
|Y\_l|\_F
}
]

\---

## 49.4 Cosine Similarity

\[
C\_l=
\\frac{
Y\_l^T\\hat Y\_l
}{
|Y\_l|
|\\hat Y\_l|
}
]

\---

## 49.5 Attention Error

比较：

\[
A
===

softmax(QK^T)
]

和

\[
\\hat A
]

可统计：

* KL divergence；
* top-attended-token change；
* attention entropy change。

\---

## 49.6 Logit Error

统计：

\[
|z-\\hat z|
]

以及：

\[
Top1(z)
\\neq
Top1(\\hat z)
]

比例。

\---

## 49.7 End-to-End

最终评估：

* perplexity；
* MMLU/CMMLU；
* GSM8K/数学；
* HumanEval/代码；
* long-context；
* domain-specific tasks。

\---

# 50\. 推荐的量化误差实验矩阵

建议针对同一模型进行：

|实验|Weight|Activation|KV|目的|
|-|-|-|-|-|
|FP16 baseline|FP16|FP16|FP16|基线|
|W8A16|INT8|FP16|FP16|权重量化敏感性|
|W4A16|INT4|FP16|FP16|低比特权重|
|W4A8|INT4|INT8|FP16|激活影响|
|W4A4|INT4|INT4|FP16|极低比特矩阵乘|
|W4A4KV8|INT4|INT4|INT8|KV 初步影响|
|W4A4KV4|INT4|INT4|INT4|全链路误差|
|W4A4KV2|INT4|INT4|INT2|极限 KV 压缩|

然后逐层记录：

\[
E\_l
]

即可绘制“量化误差随层数传播曲线”。

\---

# 51\. 建议构造的三个关键指标

## 指标 1：Layer Quantization Amplification Factor

定义：

\[
QAF\_l
===

\\frac{
|\\Delta Y\_l|
}{
|\\Delta W\_l|
}
]

表示相同权重量化误差经过当前层以后被放大多少。

\---

## 指标 2：Error Propagation Gain

在第 (l) 层注入扰动：

\[
\\epsilon\_l
]

最终 logit 变化：

\[
\\Delta z
]

定义：

\[
EPG\_l
===

\\frac{
|\\Delta z|
}{
|\\epsilon\_l|
}
]

可以衡量不同层对最终输出的敏感性。

\---

## 指标 3：Token Decision Flip Rate

\[
TDFR
===

\\frac{
1
}{
T
}
\\sum\_t
\\mathbb I
\\left\[
argmax(z\_t)
\\neq
argmax(\\hat z\_t)
\\right]
]

比单纯 logit MSE 更接近生成任务中的真实影响。

\---

# 52\. 大模型量化误差的统一机理图

```text
             Quantization
                  │
       ┌──────────┼──────────┐
       │          │          │
    Weight     Activation    KV Cache
       │          │          │
       └──────┬───┴─────┬────┘
              │         │
        Linear Projection
              │
     ┌────────┴────────┐
     │                 │
 Attention           FFN
 Q×K + Softmax    SwiGLU multiply
     │                 │
     └────────┬────────┘
              │
          Residual
              │
        RMSNorm/Norm
              │
        Next Transformer
              │
            ...
              │
           LM Head
              │
      Logit Perturbation
              │
      Token Ranking Change
              │
 Autoregressive Trajectory
              │
      Task Quality Loss
```

\---

# 53\. 核心结论

本报告最终可以把“大模型架构—量化误差耦合作用机制”总结为十条：

### 结论 1

量化误差不是独立白噪声，而是：

\[
\\boxed{\\text{输入相关、通道相关、层相关、结构相关的确定性扰动}}
]

### 结论 2

线性层中的核心误差项是：

\[
\\boxed{
X\\Delta W+\\Delta XW
}
]

所以权重是否重要必须结合 activation 判断。

### 结论 3

Activation outlier 会扩大动态范围并降低普通值的有效量化分辨率，是低比特 activation quantization 的核心障碍之一。

### 结论 4

Attention 的：

\[
QK^T
+
Softmax
]

使 Q/K 量化误差可能从数值扰动转化为 attention routing 改变。

### 结论 5

SwiGLU：

\[
a\\odot b
]

形成典型的乘性误差耦合，因此 FFN activation 低比特量化具有较高难度。

### 结论 6

Residual Connection 使误差跨层传播，而自回归生成使误差跨 token 传播。

### 结论 7

KV Cache 量化与普通 activation quantization 不同，因为 KV 状态具有长期复用和时间累积特征。

### 结论 8

GQA/MQA 会让单个 K/V head 的量化误差影响多个 Q head，因此共享 KV 结构改变了误差作用范围。

### 结论 9

MoE 的 router 是高度敏感节点，小量化误差可能导致 Top-K expert 离散切换；不同 expert 应考虑混合精度。

### 结论 10

最有效的低比特量化方向，本质上是在降低：

\[
\\boxed{
\\text{有效结构误差}
===

\\text{Quantization Error}
\\times
\\text{Architecture Sensitivity}
}
]

而不仅仅是减小：

\[
|W-W\_q|
]

\---

# 54\. 对工程实践的建议

如果目标是实际部署大模型，可以采用如下优先级：

### 第一阶段：W4A16

优先：

* AWQ；
* GPTQ；
* group-wise quantization。

特点：

* 精度较稳定；
* 工程成熟；
* 容易获得显存收益。

### 第二阶段：W8A8 / W4A8

重点解决 activation outlier：

* SmoothQuant；
* per-token scaling；
* per-channel scaling。

### 第三阶段：W4A4

重点：

* rotation；
* clipping；
* mixed precision；
* sensitive layer protection。

### 第四阶段：KV4 / KV2

重点：

* K/V 分别设计量化粒度；
* per-channel K；
* per-token V；
* residual high-precision window；
* long-context evaluation。

### 第五阶段：MoE

重点：

* Router FP16/BF16；
* Expert mixed precision；
* activation frequency + sensitivity 联合 bit allocation；
* expert-aware kernel。

\---

# 55\. 推荐的研究路线

如果需要进一步研究“架构—量化误差耦合”，建议按以下路线推进。

## Step 1：建立数学扰动模型

对每个模块推导：

\[
\\Delta Y
===

J\_X\\Delta X
+
J\_W\\Delta W
]

\---

## Step 2：测量模块局部误差

分别量化：

* Q；
* K；
* V；
* O；
* Gate；
* Up；
* Down；

测量：

\[
|\\Delta Y|
]

\---

## Step 3：测量跨层传播

只量化单层 (l)，测量最终：

\[
|\\Delta logits|
]

得到：

\[
EPG\_l
]

\---

## Step 4：分析 outlier

统计：

* per-channel max；
* per-token max；
* kurtosis；
* top 0.1% activation。

\---

## Step 5：分析 Attention

测量：

\[
KL(A|\\hat A)
]

和 top-attention flip。

\---

## Step 6：分析生成轨迹

比较 FP16 与 INT4：

* 首次 token divergence position；
* divergence 之后的 logit KL；
* 长文本累积偏差。

\---

## Step 7：建立混合精度算法

根据：

\[
Sensitivity\_l
]

自动分配：

\[
2/3/4/8/16\\text{ bit}
]

形成 architecture-aware mixed precision quantization。

\---

# 56\. 最终总结

大模型量化的本质不是：

\[
FP16
\\rightarrow
INT4
]

而是：

\[
\\boxed{
\\text{连续高维神经网络}
\\rightarrow
\\text{有限精度计算系统}
}
]

真正决定低比特模型是否可用的，不只是量化器本身，而是量化误差与 Transformer 架构之间的耦合。

可以用一个统一公式概括：

\[
\\boxed{
\\Delta \\text{Output}
\\approx
\\mathcal P\_{\\text{architecture}}
(
\\Delta W,
\\Delta A,
\\Delta KV
)
}
]

其中：

\[
\\mathcal P\_{\\text{architecture}}
]

代表由：

* Attention；
* Softmax；
* SwiGLU；
* RMSNorm；
* Residual；
* RoPE；
* GQA/MQA；
* MoE routing；
* Autoregressive decoding；

共同构成的误差传播算子。

因此未来高质量低比特推理的核心方向，不应只是追求更低的 quantization MSE，而是：

\[
\\boxed{
\\min
\\text{Architecture-Weighted Quantization Error}
}
]

即：

> \*\*根据模型结构对误差的真实敏感度，重新设计量化粒度、量化位置、缩放方式、坐标基和 bit-width 分配。\*\*

\---

# 参考文献与延伸阅读

1. Dettmers, T. et al. **LLM.int8(): 8-bit Matrix Multiplication for Transformers at Scale.** arXiv:2208.07339.  
https://arxiv.org/abs/2208.07339
2. Xiao, G. et al. **SmoothQuant: Accurate and Efficient Post-Training Quantization for Large Language Models.** arXiv:2211.10438.  
https://arxiv.org/abs/2211.10438
3. Frantar, E. et al. **GPTQ: Accurate Post-Training Quantization for Generative Pre-trained Transformers.** arXiv:2210.17323.  
https://arxiv.org/abs/2210.17323
4. Lin, J. et al. **AWQ: Activation-aware Weight Quantization for LLM Compression and Acceleration.** arXiv:2306.00978.  
https://arxiv.org/abs/2306.00978
5. Dettmers, T. et al. **QLoRA: Efficient Finetuning of Quantized LLMs.** arXiv:2305.14314.  
https://arxiv.org/abs/2305.14314
6. Liu, Z. et al. **KIVI: A Tuning-Free Asymmetric 2bit Quantization for KV Cache.** arXiv:2402.02750.  
https://arxiv.org/abs/2402.02750
7. Ashkboos, S. et al. **QuaRot: Outlier-Free 4-Bit Inference in Rotated LLMs.** arXiv:2404.00456.  
https://arxiv.org/abs/2404.00456
8. Liu, Z. et al. **SpinQuant: LLM Quantization with Learned Rotations.** arXiv:2405.16406.  
https://arxiv.org/abs/2405.16406
9. **Examining Post-Training Quantization for Mixture-of-Experts.** arXiv:2406.08155.  
https://arxiv.org/abs/2406.08155
10. Chowdhury, M. N. R. et al. **Efficient Quantization of Mixture-of-Experts with Theoretical Generalization Guarantees.** arXiv:2604.06515, 2026.  
https://arxiv.org/abs/2604.06515

\---

## 附：一句话理解各类量化方法

|方法|一句话理解|
|-|-|
|RTN|直接把权重四舍五入到最近量化点|
|GPTQ|根据输入/Hessian，优先降低真正影响输出的权重量化误差|
|AWQ|激活越大的通道越需要保护其对应权重|
|SmoothQuant|把 activation 的量化困难迁移到 weight|
|LLM.int8|把异常通道单独高精度计算|
|QLoRA|4bit 冻结基座 + 高精度 LoRA 学习|
|KIVI|根据 K/V 不同分布分别设计 KV Cache 量化粒度|
|QuaRot|用正交旋转打散 outlier，使低比特量化更容易|
|SpinQuant|进一步学习最有利于量化的旋转基|
|Mixed Precision|把更多 bit 分给对最终输出更敏感的结构|

\---

**报告结束。**

